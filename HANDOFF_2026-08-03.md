# Handoff — paste into a new Cursor session

**Date:** 2026-08-03  
**Owner:** Jeff (Kepi Travel)  
**Repo:** `jpro99/Kepi-Travel` → production **kepitravel.com** (Vercel)  
**Branch:** `main` (clean / in sync with origin as of this handoff)  
**Latest ship:** Airport Day Coach Task A (time-budget + full-day toggle on departure fallback). Prior: `8bafb8b` F14 Book advisor.

Read first: `KEPI_PROJECT_MEMORY.md`, `NEURO_BRAIN.md`, `KEPI_DESIGN_LAW.md`.  
Jeff Mode: **discuss before code** unless he says go / build / implement / ship.

---

## Where we are (product)

Kepi is a **trip brain + day-of coach**, not a classic OTA.

- Most flights/hotels: **compare in Kepi → book on Google/airline/Booking → forward confirmation**.
- Stripe: Pro/Concierge subs + some in-app hotel checkouts (LiteAPI when competitive).
- Flights “pay in Kepi”: Duffel balance path exists in Command Deck; Book search is **handoff**, not locked OTA prices.

---

## Shipped this stretch (do not redo)

| Commit | What |
|--------|------|
| `ac45ef9` | Email ingest audit F6/F7/F9–F12 (gate before promote, drain default-deny, etc.) |
| `b360cab` | Lifetime invite unlock UX (preserve code, banner, retry) |
| `037927a` | Plan B Home — Next up + import celebration |
| `bc4b695` | **I46** Calendar: stop Munich stay past return; return day = home to Ontario via Rome |
| `8bafb8b` | **F14** Book flights = advisor Top picks + honest Google/Seats CTAs |

### F14 Book flights (just shipped)

- Modal uses `POST /api/flights/advisor-search` → fused search + genome origins (ONT/PSP/SoCal).
- UI: **Top picks** (overall / cash / miles / Alaska) in `TripFlightSearch.tsx`.
- Helper: `src/lib/flights/bookFlightAdvisorPicks.ts` (+ tests).
- No `$` on external buttons; filter **Duffel Airways**; quote = “confirm on Google.”
- Full Command Deck (TVS/topology/in-app Duffel pay) stays **off** Book.

### I46 Calendar

- `fillCoverageGaps` does not paint Munich after return travel day.
- Walkthrough: “Heading home to Ontario” / via Rome — not “off to Rome.”

---

## Airport Day Coach — Task A shipped; Task B blocked

**Shipped (Task A):** Departure `AirportNavigatorFallback` — time-budget pill (“Xm until departure · plenty of time / on track”; none under 45m); Full day / Coach view toggle (parent state in `AirportNavigatorMap`); coach collapses to current+next including check-in. Helpers/tests: `airportDayCoach.ts`.

**Blocked (Task B — arrival):** Do **not** build until Jeff answers mode derivation: explicit `mode: "depart"|"arrive"` from `journeyPhase` / which IATA you’re at, vs live status `"landed"`, vs both. Orphan `ArrivalMode.tsx` exists unused — absorb or delete when B ships. Map expansion still frozen.

**Plan:** `C:\Users\Jeff Russell\.cursor\plans\airport_day-of_honesty_3eb8714f.plan.md`

---

## Standing rules agents keep breaking

1. **Verify-first** — no guessed coords/IDs/API fields (`.cursor/rules/50-verify-first-no-guessing.mdc`).
2. **Never put a locked-looking $ on a Google/airline handoff button** (F14).
3. **No walk line unless `routeGrade: "surveyed"`** (M30) — SEA only today.
4. **Discuss before code** until Jeff approves.
5. After coding: lint + build → commit/push `main` → `npx vercel --prod` (Jeff preference / project memory). User git rule sometimes says ask before commit — **Jeff’s project memory wins: auto-push after ship.**
6. Airports: freeze expansion unless he reopens.

---

## Good next-session starters (Jeff picks)

1. **Implement Universal Airport Day Coach** in `AirportNavigatorFallback.tsx` (mockups in the plan file).
2. QA Europe 2026 calendar return day + Book Top picks on prod after hard refresh.
3. Something else Jeff names — do not assume airports or Command Deck rebuild.

---

## Key files

| Area | Path |
|------|------|
| Book advisor UI | `src/components/travelAssistant/TripFlightSearch.tsx` |
| Advisor API | `src/app/api/flights/advisor-search/route.ts` |
| Top picks | `src/lib/flights/bookFlightAdvisorPicks.ts` |
| Fused search | `src/lib/flights/fusedFlightSearch.ts` |
| Calendar legs | `src/lib/travelAssistant/buildTripLegs.ts` |
| Day walkthrough | `src/lib/travelAssistant/dayWalkthrough.ts` |
| No-layout airport UI | `src/components/travelAssistant/AirportNavigatorFallback.tsx` |
| Layout registry | `src/lib/airportNav/getLayout.ts` |
| Older airport map resume | `RESUME_PROMPT_airport_map_session.md` (stale on SEA-only detail; prefer this handoff + master prompt) |

---

## Paste prompt for new session

```
Read HANDOFF_2026-08-03.md, KEPI_PROJECT_MEMORY.md, and NEURO_BRAIN.md.
main is at 8bafb8b (F14 Book advisor). I46 calendar fix is live.
Airport Day Coach for unbundled airports was discussed with mockups but NOT built —
plan: airport_day-of_honesty_3eb8714f.plan.md. Airport map expansion still frozen.
Discuss before code unless I say go. What should we do next?
```
