import { notFound } from "next/navigation";

/** Sci-fi Guardian prototype — hidden from product (10/10 focus). Use real Settings → security elsewhere. */
export default function SecuritySettingsPage() {
  notFound();
}
