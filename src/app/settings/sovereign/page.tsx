import { notFound } from "next/navigation";

/** Sci-fi prototype — hidden from product (10/10 focus). Code retained under src/lib/sovereign. */
export default function SovereignSettingsPage() {
  notFound();
}
