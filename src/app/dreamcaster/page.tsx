import { notFound } from "next/navigation";

/** Sci-fi prototype — hidden from product (10/10 focus). Code retained under src/lib/dreamcaster. */
export default function DreamcasterPage() {
  notFound();
}
